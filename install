#!/usr/bin/env bash
set -euo pipefail

tmpscript="$(mktemp)"
trap 'rm -f "$tmpscript"' EXIT

curl -fsSL https://raw.githubusercontent.com/webforspeed/bono/main/install.sh -o "$tmpscript"
bash "$tmpscript" "$@"
